# AGENT BOOTSTRAP

## 每次启动必须执行

1. 读取当前目录下 `plan.md` 文件；如果没有就创建一个。
2. 分析 `plan.md` 内容，梳理接下来需要执行的任务。
3. 将当前需要执行的计划或内容写入 `plan.md`。
4. 已执行、未执行、待执行内容都要及时同步到 `plan.md`。

## 写 SKILL 注意事项

1. `SKILL.md` 文件必须以中文为主，且不能乱码。
2. 每次增删改要注意，不能删除有用的 skill 信息。
